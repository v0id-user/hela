{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://web-production-f24fc.up.railway.app/schemas/wire/message.schema.json",
  "title": "Message",
  "description": "A single published message, as it arrives on a subscriber. Canonical shape emitted by Hela.Chat.Message.to_wire/1.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "channel", "author", "body", "node", "inserted_at"],
  "properties": {
    "id": {
      "type": "string",
      "description": "UUIDv7. First 48 bits are unix-ms; lexicographic order = chronological.",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "channel": {
      "type": "string",
      "description": "The logical channel name (no project prefix)."
    },
    "author": {
      "type": "string",
      "description": "Opaque author identifier. Whatever the publisher passed."
    },
    "body": {
      "type": "string",
      "description": "The payload. Up to 4KB."
    },
    "reply_to_id": {
      "type": ["string", "null"],
      "description": "If set, the id of the message this is a reply to. Same UUIDv7 shape.",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "node": {
      "type": "string",
      "description": "Erlang node name that accepted the publish. Mostly for ops; clients can ignore."
    },
    "inserted_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp. Same time as the id's embedded ms, to microsecond precision."
    }
  }
}

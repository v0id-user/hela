##
## Copy to terraform.tfvars (gitignored) and fill in. Alternatively set
## as environment variables prefixed with TF_VAR_.
##
## Per-service env var values (POLAR_*, DATABASE_URL, secrets, etc.) are
## NOT inputs to Terraform — they are seeded into Railway via CLI. See
## `infra/railway/README.md` for the matrix and commands.
##

project_name  = "hela"
workspace_id  = "30ae0030-123a-4f5c-87b9-03de3b9a9f12"
github_repo   = "v0id-user/hela"
github_branch = "main"

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hela.dev/schemas/wire/history_request.schema.json",
  "title": "HistoryRequest",
  "description": "Cursor-paginated history query. `before` is a message id from the previous page; omit to get the latest N.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "before": {
      "type": "string",
      "description": "Cursor — UUIDv7 of the oldest message on the previous page. Inclusive start, exclusive `before`.",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 50
    }
  }
}
